# Python Sessions

These facades wrap the lower-level Rust-backed session objects and provide the
main scripting surface for live and headless workflows.

::: atomic_kernels.viewer._session
    options:
      members:
        - ViewerSessionFacade
        - PreparedHeadlessRenderFacade
