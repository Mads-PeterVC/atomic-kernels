# API Documentation

This section combines two reference formats:

- Python API pages are generated from the `atomic_kernels` Python facade.
- Rust API pages are linked from the generated `rustdoc` output for `ak-core`
  and `ak-vis`.

The split keeps each language documented in its native format while presenting
both entry points from the same docs site.
