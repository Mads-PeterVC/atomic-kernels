[package]
name = "ak-neighborlist-py"
version.workspace = true
edition = "2024"
license = "Apache-2.0"
publish = false

[lib]
name = "_ak_neighborlist"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.27.0", features = ["extension-module"] }
numpy = { version = "0.27" }
ak-core = { path = "../ak-core" }
ak-neighborlist = { path = "../ak-neighborlist" }
