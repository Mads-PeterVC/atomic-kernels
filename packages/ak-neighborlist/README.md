# ak-neighborlist

Neighbor-list utilities for atomic structures.

## Benchmarks

The deterministic periodic and non-periodic benchmark dataset, correctness
checker, and timing runner are documented in
[`benchmarks/README.md`](benchmarks/README.md). Benchmark execution is manual;
it is not part of CI and does not write result artifacts.
